const Pi=Math.PI;
const p=Math.exp(10);
const l=Math.log10(23);
const r=Math.random();

export let usuario="jaime";
export let password="qwerty";
export default function saludar(name) {
    console.log(`Buenos dias soy ${name}`);
}

export {Pi, p, l, r};

